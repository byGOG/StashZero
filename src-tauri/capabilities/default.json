{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions actually used by the StashZero frontend.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-open",
      "scope": [
        {
          "schemes": ["http", "https"]
        }
      ]
    }
  ]
}
